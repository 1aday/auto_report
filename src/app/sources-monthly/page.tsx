"use client"

// Temporary: reuse the weekly Sources page until we adapt this to monthly.
// This gives us identical UI/UX while we wire monthly data/views.
import WeeklySourcesPage from "../sources/page"

export default function SourcesMonthlyPage() {
  return <WeeklySourcesPage />
}

